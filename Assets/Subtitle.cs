using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Subtitle : MonoBehaviour
{
    /*
    [SerializeField]
    SubtitleArea subtitleArea;
    [SerializeField]
    SubtitleManager manager;
    private void OnTriggerEnter(Collider other)
    {
        if (other.gameObject.CompareTag("Player"))
        {
            if (subtitleArea.withHiero)
            {
                manager.PlaySubtitle(subtitleArea.subtitleText, subtitleArea.subtitleTime, true);
            }
            else
            {
                manager.PlaySubtitle(subtitleArea.subtitleText, subtitleArea.subtitleTime, false);
            }
            this.gameObject.SetActive(false);
        }
    }
    */
}
